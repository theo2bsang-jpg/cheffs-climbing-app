# Example environment variables for production
# NODE_ENV should be "production" in production
NODE_ENV=production
PORT=4000
# Strong secret used to sign JWTs - MUST be set in production
JWT_SECRET=your-very-strong-random-secret
# Admin bootstrap (only used on first DB init). In production, set ADMIN_PASSWORD.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-before-prod
# Comma-separated list of allowed origins for CORS (include your frontend origin)
ALLOWED_ORIGINS=https://your-domain.example
# Where to store sqlite DB inside container/host
DATA_DIR=/var/data/data.db
# Optional: cookie names
COOKIE_NAME=cheffs_token
REFRESH_COOKIE_NAME=cheffs_refresh
