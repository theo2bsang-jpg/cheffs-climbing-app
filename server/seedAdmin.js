/** Deprecated: admin seeding handled automatically in db.js on first run. */
// If you need to create or promote an admin manually, run the register endpoint
// and then PATCH /api/users/:email as an admin, or set ADMIN_EMAIL and ADMIN_PASSWORD
// before the first server start.

export default {};
